Require `datasets>=4` and `pandas>=2.2`. The `how-it-works` notebook failed to load the Chronos datasets from the Hub with older `datasets` releases (`Feature type 'List' not found`), and the package maps the `ME` month-end alias that pandas only accepts from 2.2 on.
