Require `pandas>=2.2`. The package maps the `ME` month-end alias, which older pandas releases reject with `Invalid frequency: ME`; the old floor of 2.1.2 only held because a transitive dependency happened to pull pandas higher.
