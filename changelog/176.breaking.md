`LOCAL` mode now uses `tabpfn`'s default model instead of pinning the finetuned TabPFN-TS-3 checkpoint; with `tabpfn>=9.0.0` (now the minimum) that is **TabPFN-3.5**. Pass `tabpfn_model_config={"model_path": "tabpfn-v3-regressor-v3_20260506_timeseries.ckpt"}` to keep TabPFN-TS-3.
